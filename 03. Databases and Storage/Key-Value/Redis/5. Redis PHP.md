```shell
apt-get install php-redis
```

```dockerfile
RUN pecl install redis && docker-php-ext-enable redis
```

```php
$redis = new Redis();
$redis->connect('redis', 6379);

$redis->set('foo', 'bar');
echo $redis->get('foo');
```