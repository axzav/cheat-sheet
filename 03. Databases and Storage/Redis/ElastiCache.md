# ElastiCache

Amazon ElastiCache — это управляемый сервис от Amazon Web Services.
Managed-сервис, который запускает Redis-совместимый движок (или Memcached) в AWS.
ElastiCache = инфраструктура + управление + autoscaling + failover + monitoring.
