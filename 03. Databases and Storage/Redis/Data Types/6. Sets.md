# Sets

Уникальные неупорядоченные элементы

```redis
SADD tags redis php
SISMEMBER tags redis
SMEMBERS tags
SINTER set1 set2
```

```php
$redis->sAdd('tags', 'redis', 'php');
$exists = $redis->sIsMember('tags', 'redis');
$all = $redis->sMembers('tags');
```
