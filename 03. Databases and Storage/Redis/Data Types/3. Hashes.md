# Hashes

```redis
HSET user:1 name "Alex" age 30
HGET user:1 name
HGETALL user:1
HINCRBY user:1 age 1
```

```php
$redis->hSet('user:1', 'name', 'Alex');
$redis->hSet('user:1', 'age', 30);

$name = $redis->hGet('user:1', 'name');
$user = $redis->hGetAll('user:1');
```