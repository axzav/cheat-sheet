# Design Patterns

## Порождающие (creational)
Шаблоны абстрагирующие процесс инстанцирования объектов

- **Factory Method** - абстрагирует процесс инстанцирования объектов с общим супертипом
- **Abstract Factory** - абстрагирует процесс инстанцирования семейства связанных объектов
- **Builder** - позволяет разбить процесс создания сложного объекта на шаги
- **Prototype** - предоставляет клиентскому коду метод для клонирования объекта
- **Одиночка (Singleton)**: гарантирует, что класс имеет только один экземпляр, и предоставляет к нему глобальную точку доступа.

## Структурные (structural)

- **Decorator** - позволяет добавлять новую функциональность посредствам классов-оберток с вложенной структурой. **Создает расширенный интерфейс**
- **Bridge** - позволяет разбить сложную иерархию на несколько независимых
- **Composite** - позволяет организовать древовидную структуру и работать с ней как с единым объектом

- **Adapter** - позволяет использовать в коде класс с неподходящим интерфейсом при помощи создания обертки с нужным интерфейсом. **Адаптирует объект к существующему интерфейсу**
- **Facade** - служит для предоставления простого интерфейса в рамках одного объекта для сложной подсистемы или нескольких подсистем (библиотеки или библиотек). **Создает новый интерфейс**.
- **Proxy** - позволяет добавить новую функциональность методам объекта путем создания обертки над этим объектом. **Сохраняет интерфейс без изменений**
- **Легковес (Flyweight)** - используется для уменьшения затрат на создание большого количества мелких похожих объектов.

## Поведенческие (behavioral)

Поведенческие шаблоны объединяет то, что все они выносят какую-то логику в отдельные объекты.

- **Chain Of Responsibility** - проводит запрос чарез цепочку обработчиков. Обработчик решает, пройдет ли запрос дальше по цепочке или оборвется на конкретном обработчике
- **Команда (Command)**: превращает запросы в объекты, позволяя передавать их как аргументы при вызове методов, ставить запросы в очередь, логировать их, а также поддерживать отмену операций.
- **Интерпретатор (Interpreter)**: задаёт грамматику для языка и интерпретатор для его грамматики.
- **Итератор (Iterator)**: предоставляет способ последовательного доступа ко всем элементам агрегата без раскрытия его внутреннего представления.
- **Посредник (Mediator)**: уменьшает сложность взаимодействия между объектами, введя между ними посредника.
- **Хранитель (Memento)**: позволяет сохранять и восстанавливать предыдущее состояние объекта без раскрытия деталей его реализации.
- **Наблюдатель (Observer)**: создаёт механизм подписки, позволяющий одним объектам следить и реагировать на события, происходящие в других объектах.
- **Состояние (State)**: позволяет объекту изменять своё поведение в зависимости от своего состояния.
- **Стратегия (Strategy)**: определяет семейство алгоритмов, инкапсулирует каждый из них и обеспечивает их взаимозаменяемость.
- **Шаблонный метод (Template Method)**: определяет скелет алгоритма в методе, перекладывая ответственность за некоторые его шаги на подклассы.
- **Посетитель (Visitor)**: позволяет добавлять в программу новые операции, не изменяя классы объектов, над которыми эти операции могут выполняться.


## Links
- Реальные примеры а коде Java - https://stackoverflow.com/questions/1673841/examples-of-gof-design-patterns-in-javas-core-libraries/2707195#2707195
- https://refactoring.guru/ru/design-patterns/php - список GoF паттернов с объяснением и примерами
