See git repo configs:
```shell
git config --list --show-origin
```

Set username and email
```shell
git config user.name "Username"
git config user.email "user@email.com"
```

Show remote
```shell
git remote -v
```